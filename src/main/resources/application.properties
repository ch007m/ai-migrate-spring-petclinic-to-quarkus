# Datasource - H2 default (Dev Services will auto-provision in dev/test mode)
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:petclinic
quarkus.datasource.username=sa
quarkus.datasource.password=

# Hibernate ORM
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
quarkus.hibernate-orm.fetch.batch-size=16

# SQL init - use import.sql for Quarkus
quarkus.hibernate-orm.sql-load-script=import.sql

# Logging
quarkus.log.category."org.springframework".level=INFO

# HTTP
quarkus.http.port=8080
