# Datasource - H2 by default
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:petclinic
quarkus.datasource.username=sa
quarkus.datasource.password=

# Hibernate ORM
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
quarkus.hibernate-orm.jdbc.statement-batch-size=16

# SQL init - seed data (schema is generated by Hibernate from entities)
quarkus.hibernate-orm.sql-load-script=db/h2/data.sql

# Internationalization
quarkus.default-locale=en

# Logging
quarkus.log.category."org.springframework".level=INFO

# Qute templating
quarkus.qute.strict-rendering=false
quarkus.qute.property-not-found-strategy=output-original

# Static resources are served from META-INF/resources/
